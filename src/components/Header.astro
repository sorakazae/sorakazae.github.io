---
// props 필요하면 받을 수 있음
---

<header class="header">
  <div class="header-left">
    <a href="/" class="logo">
      <img src="/sAz_logo.png" alt="saz logo" class="logo-image" />
      <span class="logo-text">saz.log</span>
    </a>
  </div>

  <form action="/search" class="search-form">
    <input
      type="search"
      name="q"
      class="search-input"
      placeholder="Search…"
    />
  </form>

  <nav class="nav">
    <a href="/blog" class="nav-link">Blog</a>
    <a href="/about" class="nav-link">About</a>
  </nav>
</header>

<style>
  .header {
  position: sticky;
  top: 0;
  z-index: 20;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0.75rem 1.5rem;
  border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  backdrop-filter: blur(16px);
  background: rgba(10, 10, 15, 0.8);
}

.header-left {
  display: flex;
  align-items: center;
  gap: 0.5rem;
}

.logo {
  display: flex;
  align-items: center;
  gap: 0.6rem;
  text-decoration: none;
  color: #00bcd4;
}

.nav {
  display: flex;
  gap: 1.5rem;
}

.search-form {
  position: absolute;
  left: 50%;
  transform: translateX(-50%);
  display: flex;
  align-items: center;
}

.search-input {
  padding: 0.25rem 0.9rem;
  border-radius: 999px;
  border: 1px solid rgba(0, 188, 212, 0.4);
  background: rgba(0, 0, 0, 0.4);
  color: #e5faff;
  font-size: 0.9rem;
  min-width: 440px;
}

.search-input::placeholder {
  color: rgba(229, 250, 255, 0.6);
}

.search-input:focus {
  outline: none;
  border-color: #00bcd4;
  background: rgba(0, 0, 0, 0.7);
}

.nav-link {
  font-size: 0.95rem;
  opacity: 0.85;
  text-decoration: none;
  color: #00bcd4;
}

.logo-image {
  width: 36px;
  height: 36px;
  border-radius: 999px;
}

.logo-text {
  font-weight: 700;
  font-size: 1.25rem;
}

.nav-link:hover {
  opacity: 1;
}
</style>